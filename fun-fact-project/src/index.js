import React from 'react';
import ReactDOM from 'react-dom';
import App from './App';

// render a React element in the DOM
ReactDOM.render(
    <App />,
    document.getElementById("root") // identify the HTML element where the render occurs
);