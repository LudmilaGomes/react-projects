// create a react component (javascript class or function)

// PascalCasing (each first character of a word is uppercase)
function Message() {
    // JavaScript XML (JSX)
    const name = 'Ludmila';
    if (name)
        return <h1>Hello, {name}!</h1>;
    return <h1>Hello, World!</h1>;
}

export default Message;